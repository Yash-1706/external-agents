[tasks.build]
description = "Build the binary"
run = "GOCACHE=/tmp/go-build-cache go build -o entire-agent-acmecode ./cmd/entire-agent-acmecode"

[tasks.test]
description = "Run unit tests"
run = "GOCACHE=/tmp/go-build-cache go test ./..."

[tasks.clean]
description = "Remove built binary"
run = "rm -f entire-agent-acmecode"
